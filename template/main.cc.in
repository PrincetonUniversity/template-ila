// File: main.cc

#include <iostream>

#include <${project_name}/${header_file_name}>

using namespace ilang;

int main() {
  auto m = ${project_name}::${top_method_name}("${project_name}");
  std::cout << "Successfully construct " << m << "\n";
  return 0;
}
